# Lint After Editing JavaScript

After modifying any `.js` or `.jsx` files in `src/` or `example/`, run the linter:

```bash
yarn lint
```

This lints both `src/` and `example/` directories.

**If the linter reports errors, you MUST fix them before considering the task complete.**

Warnings should also be addressed when reasonable.

# Run Tests After Modifying Code

After modifying any `.js` or `.jsx` files in `src/`, run the tests:

```bash
yarn test:run
```

**If any tests fail, you MUST fix them before considering the task complete.**

This ensures changes don't break existing functionality.
